Run `tetris` for the online version.
If the online version fails due to some JSON parsing error, you should probably run the offline version (`tetris_offline`).

The offline version will work forver, the online version only works if I decide to keep the server running :)
